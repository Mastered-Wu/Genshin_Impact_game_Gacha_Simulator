# AGENTS.md

本文件是本项目给所有接手者（包括人类开发者和 AI 代理）的协作规范。开始任何实现、重构、测试或文档工作前，必须先阅读并遵守本文件。

## 项目定位

本项目是一个本地抽卡模拟器，目标是在不使用官方名称、美术、Logo 或联网数据的前提下，模拟《原神》风格的祈愿机制。

权威需求来源：

- 设计说明：`docs/superpowers/specs/2026-08-31-gacha-simulator-design.md`
- 实现计划：`docs/superpowers/plans/2026-08-31-gacha-simulator.md`

如果实现细节与上述文档冲突，先以设计说明为准；如果仍有歧义，应先提出问题，不要擅自扩大范围。

## 不可违反的范围约束

- 所有项目文件必须保留在 `C:\Users\27415\Desktop\抽卡模拟器` 内。
- 后端逻辑必须使用 C++。
- 前端必须使用纯 HTML/CSS/JavaScript。
- 第一屏必须是可直接使用的模拟器，不做营销落地页。
- 第一版不实现软保底，除非用户明确要求变更范围。
- 第一版不包含用户账号、支付、实时游戏数据抓取、官方美术、官方 Logo 或远程联网后端。
- 必须使用原创占位名称和资源，不得引入官方游戏物品名、图片、Logo 或受版权限制的素材。
- 生成的构建产物、日志、临时文件和二进制文件不得提交到 Git。

## 核心规则必须保持正确

角色活动祈愿：

- 5 星基础概率为 0.6%。
- 第 90 抽硬保底获得 5 星。
- 第 10 抽硬保底获得 4 星或以上。
- 无限定保底时，5 星有 50% 概率为限定角色。
- 歪掉后，下一个角色活动 5 星必须为限定。
- 角色活动祈愿状态独立于武器和常驻祈愿。

武器活动祈愿：

- 5 星基础概率为 0.7%。
- 第 80 抽硬保底获得 5 星。
- 第 10 抽硬保底获得 4 星或以上。
- 无限定保底时，5 星有 75% 概率为两把限定 5 星武器之一。
- 歪掉后，下一个武器活动 5 星必须为限定武器。
- 选择定轨后，获得非所选 5 星时命定值加 1。
- 命定值达到 2 后，下一个 5 星必须为所选武器。
- 获得所选武器、更改定轨或清除定轨时，命定值必须重置为 0。

常驻祈愿：

- 5 星基础概率为 0.6%。
- 第 90 抽硬保底获得 5 星。
- 第 10 抽硬保底获得 4 星或以上。
- 第一版不包含限定保底。

## 架构规范

- `WishEngine` 是纯规则引擎，不得依赖 HTTP、前端、文件系统或 UI。
- 概率、保底、限定保底和命定值逻辑必须集中在后端 C++ 规则层。
- HTTP/API 层只负责请求解析、状态管理、JSON 响应和静态文件服务。
- 前端只通过 API 使用后端状态，不在浏览器中重复实现核心抽卡规则。
- 随机数必须通过 `RandomProvider` 抽象注入，测试使用确定性随机序列。
- 卡池配置应保持可读、可替换。即使后端第一版使用内置数据，也要维护 `configs/banners.json` 作为项目配置资料。

## API 约定

本地服务监听 `127.0.0.1`，并提供以下接口：

- `GET /api/state`
- `POST /api/wish`
- `POST /api/path`
- `POST /api/reset`

错误响应必须是 JSON，并包含稳定的 `code` 字段，例如：

- `unknown_banner`
- `invalid_count`
- `invalid_path_item`

不要随意修改 API 字段名、卡池 id 或错误码；如需修改，必须同步更新前端、测试、README 和设计文档。

## 前端规范

- UI 文案使用中文。
- 页面打开后应直接进入可操作模拟器。
- 必须支持单抽和十连。
- 主抽卡界面不展示保底计数器或命定值计数器，尽量给卡池背景和抽卡氛围留空间；用户可通过“查看历史”自行计算保底。
- 顶部资源区必须展示并允许修改星石数量；星轨之缘和恒辉之缘只读展示，不允许前端直接修改。
- 必须展示抽卡历史和统计信息。
- 武器定轨选择器只应在武器活动祈愿中显示。
- 界面应兼容桌面和移动端。
- 不使用外部素材或网络资源。
- 样式应清晰、稳定，按钮和结果区域不要因内容变化产生明显布局跳动。

## 测试规范

后端测试必须至少覆盖：

- 角色活动祈愿第 90 抽硬保底。
- 角色活动祈愿 50/50 歪掉后，下一个 5 星必定为限定。
- 所有卡池第 10 抽 4 星或以上硬保底。
- 武器活动祈愿第 80 抽硬保底。
- 武器活动祈愿歪掉后，下一个 5 星必定为限定武器。
- 武器命定值达到 2 后强制获得所选武器。
- 获得所选武器、更改定轨或清除定轨后命定值重置。
- 常驻祈愿第 90 抽硬保底。
- API 冒烟测试至少验证默认状态包含三个卡池，以及非法抽卡次数返回 `invalid_count`。

前端或端到端验证至少覆盖：

- 服务器能启动并提供页面。
- 单抽能更新结果、历史和资源显示。
- 十连能返回并展示 10 条结果。
- 武器定轨选择器只在武器卡池显示。
- 重置能清空计数器和历史。

## 推荐工作流程

1. 先阅读本文件、设计说明和实现计划。
2. 用 `git status --short` 检查工作区，确认是否已有他人改动。
3. 按实现计划中的任务顺序推进，不要跳过测试。
4. 对规则层改动，先补充或更新确定性测试，再实现。
5. 每个任务完成后运行对应测试。
6. 只提交与当前任务相关的文件。
7. 提交前再次运行 `git status --short`，确认没有构建产物或无关文件被纳入。

常用命令：

```powershell
cmake -S backend -B build
cmake --build build
.\build\gacha_core_tests.exe
.\build\api_smoke_tests.exe
```

项目脚本创建后，优先使用：

```powershell
.\scripts\test.ps1
.\scripts\run.ps1
```

## Git 与协作规范

- 不要回滚、删除或覆盖不是自己本轮任务产生的改动。
- 不要提交 `build/`、CMake 中间文件、可执行文件、日志或临时文件。
- 临时测试脚本、一次性验证脚本和后续用不到的测试辅助文件必须在完成验证后从工作区删除干净；保留的测试脚本必须是项目长期维护入口。
- 每个提交应聚焦一个任务或一个明确修复。
- 工程级别的实现、重构、测试、文档或配置修改完成并验证后，必须及时提交到 Git；除非用户明确要求暂不提交，不得只停留在未提交工作区。
- 推荐提交信息：
  - `feat: add gacha wish engine`
  - `feat: add local gacha API server`
  - `feat: add simulator frontend`
  - `fix: polish simulator integration`
- 如果当前工作区有未知改动，先确认其用途；无关改动保持原样。

## 文档维护规范

- 修改需求、规则、API 或运行方式时，必须同步更新相关文档。
- 设计说明记录“应该做什么”和“为什么”。
- 实现计划记录“按什么顺序做”和“如何验证”。
- `README.md` 面向普通使用者，说明构建、运行和测试。
- `AGENTS.md` 面向接手项目的开发者和 AI，记录必须遵守的协作规则。

## 禁止事项

- 禁止引入官方游戏素材、官方物品名、官方 Logo 或抓取官方实时数据。
- 禁止把核心抽卡规则只写在前端。
- 禁止用统计随机测试替代确定性规则测试。
- 禁止在第一版中悄悄加入软保底、账号、支付、远程服务等超范围功能。
- 禁止为了让测试通过而削弱规则正确性。
- 禁止提交生成产物或本地环境文件。
